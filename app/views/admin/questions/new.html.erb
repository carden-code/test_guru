<h1><%= question_header(@question) %></h1>
<%= render 'shared/errors', resource: @question %>

<%= form_for [:admin, @test, @question] do |form| %>

  <%= form.label :body %>
  <%= form.text_field :body %>

  <%= form.submit %>

<% end %>

<%= link_to 'Back', admin_test_path(@test) %>
